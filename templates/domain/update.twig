// find
$model = $this->get($id);

if ($model === null) {
	return new NotFound(['message' => '{{ class }} not found.']);
}

// hydrate
$serializer = {{ class }}::getSerializer();
$model = $serializer->hydrate($model, $data);
$this->hydrateRelationships($model, $data);

// validate
$validator = $this->getValidator();
if ($validator !== null && !$validator->validate($model)) {
	return new NotValid([
		'errors' => $validator->getValidationFailures()
	]);
}

// dispatch
$event = new {{ class }}Event($model);
$this->dispatch({{ class }}Event::PRE_UPDATE, $event);
$this->dispatch({{ class }}Event::PRE_SAVE, $event);
$rows = $model->save();
$this->dispatch({{ class }}Event::POST_UPDATE, $event);
$this->dispatch({{ class }}Event::POST_SAVE, $event);

$payload = ['model' => $model];

if ($rows === 0) {
	return new NotUpdated($payload);
}

return new Updated($payload);