// find
$model = $this->get($id);

if ($model === null) {
	return new NotFound(['message' => '{{ class }} not found.']);
}

// update
if ($this->doSet{{ related_name }}Id($model, $relatedId)) {
	$this->dispatch({{ class }}Event::PRE_{{ const }}_UPDATE, $model);
	$this->dispatch({{ class }}Event::PRE_SAVE, $model);
	$model->save();
	$this->dispatch({{ class }}Event::POST_{{ const }}_UPDATE, $model);
	$this->dispatch({{ class }}Event::POST_SAVE, $model);

	return Updated(['model' => $model]);
}

return NotUpdated(['model' => $model]);