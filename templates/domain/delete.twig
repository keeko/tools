// find
$model = $this->get($id);

if ($model === null) {
	return new NotFound(['message' => '{{ class }} not found.']);
}

// delete
$event = new {{ class }}Event($model);
$this->dispatch({{ class }}Event::PRE_DELETE, $event);
$model->delete();

if ($model->isDeleted()) {
	$this->dispatch({{ class }}Event::POST_DELETE, $event);
	return new Deleted(['model' => $model]);
}

return new NotDeleted(['message' => 'Could not delete {{ class }}']);