$body = Json::decode($request->getContent());
if (!isset($body['data'])) {
	throw new InvalidParameterException();
}
$data = $body['data'];
$id = $this->getParam('id');
$domain = new {{ domain }}($this->getServiceContainer());
$payload = $domain->add{{ foreign_class }}($id, $data);
return $this->responder->run($request, $payload);