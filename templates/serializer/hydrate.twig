// attributes
$attribs = isset($data['attributes']) ? $data['attributes'] : [];

{% if normalizer %}$service = $this->getServiceContainer();
{% endif %}
$model = HydrateUtils::hydrate($attribs, $model, {{ code|raw }});

// relationships
//$this->hydrateRelationships($model, $data);

return $model;